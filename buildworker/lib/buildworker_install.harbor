## Install buildworker
RUN mkdir -p /buildworker
WORKDIR /buildworker

RUN $L32 pip install buildbot-worker
RUN buildbot-worker create-worker --keepalive=100 --umask 022 worker ${buildbot_server}:${buildbot_port} ${buildworker_name} ${buildbot_password}
